<%= content_for :dashboard_class, "active" %>

<% if can? :view_projects, User %>
   <%= list_report("Developers", {collection: @developers, status: @statuses}) %>
   <%= list_report("Projects", {collection: @projects, status: @statuses}) %>
<% end %>
